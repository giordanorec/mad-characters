Create a sleek magazine-style infographic (editorial, modern, Information-is-Beautiful vibe), landscape, dark theme (charcoal background, neon-teal and amber accents), flat-vector with subtle glow, sharp legible sans-serif typography.

TITLE: "Guarantees, not suggestions". Subtitle: "a deterministic state machine governs the flow - the AI only fills the nodes".

Central visual: a robust gate/checkpoint metaphor - a pipeline where an AI action tries to pass and a GATE either lets it through (green) or BLOCKS it (red), with a shield icon. Around it, 4 labeled cards showing the safety layers:
- "Constitution" (a scroll with rules)
- "Gates block, not suggest" (a barrier)
- "Tests must really pass" (a green test tube / CI check)
- "Least-privilege agents" (a key / lock per worker)
Bottom banner tagline: "prose is a suggestion; a hook that blocks is a guarantee."

Poster-quality, high detail, cohesive, very readable real text labels, balanced layout with whitespace.
